# This workflow is superseded by ci.yml which uses Ruff for formatting checks.
# Ruff is a drop-in replacement for Black and runs ~100x faster.
#
# Keeping this file to avoid breaking any external references, but it is
# effectively disabled — no jobs are defined here.
#
# See: .github/workflows/ci.yml
name: "(disabled) black-action — replaced by ci.yml"
on: []
jobs: {}
