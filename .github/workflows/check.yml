# This workflow is superseded by ci.yml which provides a complete, modern
# lint + test pipeline with Ruff, pytest, and coverage reporting.
#
# Keeping this file to avoid breaking any external references, but it is
# effectively disabled — no jobs are defined here.
#
# See: .github/workflows/ci.yml
name: "(disabled) Clone Repo and Install Requirements — replaced by ci.yml"
on: []
jobs: {}
