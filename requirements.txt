# Core API / Vercel runtime dependencies only.
# All other dependencies are declared as optional extras in pyproject.toml:
#
#   pip install -e ".[gui]"        # GUI apps (PyQt5, Pillow, …)
#   pip install -e ".[games]"      # Games (pygame)
#   pip install -e ".[ml]"         # Machine learning (OpenCV, mediapipe, …)
#   pip install -e ".[automation]" # Desktop automation (pyautogui, qrcode)
#   pip install -e ".[dev]"        # Dev tools (pytest, ruff, mypy)
#   pip install -e ".[gui,ml,dev]" # Multiple extras at once

fastapi>=0.95.0
uvicorn>=0.22.0

# Snyk-pinned transitive security patches
fonttools>=4.43.0
holidays>=0.45
zipp>=4.1.0
nltk>=3.10.0
protobuf>=7.35.1
urllib3>=2.5.0
starlette>=0.49.1
soupsieve>=2.8.4
